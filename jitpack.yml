before_install:
    - sh ./scripts/jitpack_install_environment.sh
    - echo "Yarn intstall..."
    - export PATH="$HOME/.yarn/bin:$HOME/.config/yarn/global/node_modules/.bin:$PATH"
    - yarn install 

install:
    - echo "From root folder"